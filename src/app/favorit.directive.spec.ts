import { FavoritDirective } from './favorit.directive';

describe('FavoritDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoritDirective();
    expect(directive).toBeTruthy();
  });
});
